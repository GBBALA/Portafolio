import React from 'react';

const Header = () => {
  return (
    <header>
      <nav>
        <a href="#hero">Inicio</a>
        <a href="#about">Sobre Mí</a>
        <a href="#projects">Proyectos</a>
        <a href="#contact">Contacto</a>
      </nav>
    </header>
  );
};

export default Header; // <-- Verifica esta línea